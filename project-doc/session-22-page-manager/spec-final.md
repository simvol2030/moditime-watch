# Session-22: Page Manager Admin — Спецификация

**Версия:** final
**Дата:** 2026-02-19
**ASCII-спека:** `../../ASCII/admin/page-manager.md`

---

## Есть сейчас vs Должно быть

| Аспект | Есть | Должно быть |
|--------|------|-------------|
| Общий раздел "Страницы" | Нет — каждый тип контента в своём разделе | Навигационный хаб: все страницы в одном списке |
| Единичные страницы (about, contacts) | Нет UI редактирования | Ссылки на редакторы (deeplink) |
| Homepage | Отдельный раздел в admin | Отображается в списке с deeplink |
| Статьи | Раздел "Статьи" | Отображаются в списке с deeplink |
| City articles | Раздел "Города" | Отображаются в списке с deeplink |

---

## Что на выходе

Страница `/admin/pages` — центральный экран для обзора всех страниц сайта.

### Единичные страницы (нельзя удалить)

Фиксированный список:
- Главная (`/`) → deeplink `/admin/homepage`
- О сервисе (`/about`) → deeplink `/admin/pages/about` (или будущий редактор)
- Контакты (`/contacts`) → deeplink `/admin/pages/contacts`
- Доставка (`/delivery`) → deeplink `/admin/pages/delivery`
- Политика конфиденциальности (`/privacy`) → deeplink
- Оферта (`/offer`) → deeplink

Показывается: название, шаблон, URL, статус (опубл./черновик), кнопка [✏️].

### Контент-страницы (создаются/удаляются в своих разделах)

Агрегированный список из нескольких таблиц:
- Статьи (`articles`) → deeplink `/admin/articles/{id}`
- City articles (`city_articles`) → deeplink `/admin/cities/{city_id}/articles/{id}`

Показывается: название, тип (Статья/pSEO), URL, статус, кнопки [✏️] [🗑️].

Фильтр по типу + поиск по названию.

---

## Что нужно сделать

### 1. Создать страницу `/admin/pages`

**Server load:** собрать данные из нескольких источников:
- Фиксированный список единичных страниц (hardcoded array + проверка status в `pages` table)
- `articles` — выборка с пагинацией
- `city_articles` — выборка с пагинацией

**UI:**
- Две секции: "Единичные страницы" + "Контент-страницы"
- Поиск по названию (фильтрация)
- Фильтр по типу: Все / Статьи / Города
- Пагинация для контент-страниц

### 2. Deeplinks

Клик [✏️] → переход в соответствующий раздел админки:
- Homepage → `/admin/homepage`
- About/Contacts/Delivery → `/admin/pages/{slug}` (если есть редактор) или "Скоро"
- Article → `/admin/articles/{id}`
- City article → `/admin/cities` (к нужному городу)

### 3. Sidebar link

Добавить "Страницы" в admin sidebar.

---

## Факторы реализации

- **Навигационный хаб:** Page Manager не создаёт/редактирует контент — только показывает и перенаправляет
- **Единичные страницы:** список фиксирован, не из БД (хотя можно хранить статус в `pages` table)
- **Пагинация:** для articles и city_articles нужна серверная пагинация
- **Минимальный scope:** редактор для about/contacts НЕ входит — только deeplinks на существующие разделы
- **ASCII-спека:** строго `ASCII/admin/page-manager.md`

---

## Критерии успеха

- [ ] Страница `/admin/pages` отображает все страницы сайта
- [ ] Единичные страницы: фиксированный список с deeplinks
- [ ] Контент-страницы: агрегация articles + city_articles
- [ ] Поиск и фильтрация работают
- [ ] Deeplinks ведут в правильные разделы админки
- [ ] Пагинация для контент-страниц
- [ ] Ссылка "Страницы" в sidebar

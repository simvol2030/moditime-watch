# Task 1: UI — Раздельные поля CSV и ZIP — Plan

## Задачи

### 1.1 Обновить preview-форму в +page.svelte
- Заменить один input `file` на два: `csv_file` (.csv required) + `images_zip` (.zip optional)
- ZIP-поле показывать только для products
- Обновить подсказки
- DoD: Форма имеет два раздельных поля загрузки

### 1.2 Обновить import-форму в +page.svelte
- Аналогично два поля для повторного выбора
- DoD: Import-форма тоже имеет два поля

### 1.3 Обновить инструкцию (guide)
- Добавить упоминание отдельного ZIP с фото
- DoD: Инструкция отражает новый формат

### 1.4 Минимальное обновление +page.server.ts
- Принять `csv_file` вместо `file`, с fallback на `file`
- Принять `images_zip` (пока не обрабатывать — Task 2)
- DoD: Сервер принимает новые имена полей, старый CSV-only flow работает

### 1.5 CSS стили для двухполевого layout
- Grid или flex для двух полей рядом
- DoD: Поля визуально разделены, responsive

## Аудит плана
- [x] Полнота: покрыты UI preview, UI import, server fallback
- [x] Реалистичность: DoD конкретные и достижимые
- [x] Порядок: 1.1→1.2→1.3→1.4→1.5 (нет жёстких зависимостей)

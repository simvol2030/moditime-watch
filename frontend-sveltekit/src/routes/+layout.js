// Отключаем SSR для ускорения разработки
// Перед деплоем вернуть ssr: true для SEO и производительности
// Включаем SSR для SEO
export const ssr = true;

// Отключаем предварительный рендеринг
export const prerender = false;

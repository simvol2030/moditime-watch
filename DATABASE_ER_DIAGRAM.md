# ER-ДИАГРАММА - MODITIMEWATCH DATABASE

**Дата:** 20 ноября 2024
**Всего таблиц:** 36

---

## 📊 ВИЗУАЛИЗАЦИЯ СВЯЗЕЙ (Entity Relationship Diagram)

### **БЛОК 1: ГЛОБАЛЬНЫЕ НАСТРОЙКИ**

```
┌─────────────────┐
│  site_config    │ (Глобальные настройки)
├─────────────────┤
│ id              │
│ key             │
│ value           │
│ type            │
│ description     │
└─────────────────┘

┌──────────────────┐
│ navigation_items │ (Меню header/footer)
├──────────────────┤
│ id               │
│ label            │
│ href             │
│ parent_id        │──┐ self-reference
│ position         │  │ для submenu
│ menu_type        │  │
│ is_active        │  │
└──────────────────┘  │
         ▲            │
         └────────────┘

┌─────────────────┐
│    widgets      │ (Переиспользуемые блоки)
├─────────────────┤
│ id              │
│ type            │ ('telegram_cta', 'search_widget')
│ title           │
│ description     │
│ data_json       │ (JSON с конфигом виджета)
│ is_active       │
│ position        │
└─────────────────┘
```

---

### **БЛОК 2: ТОВАРЫ (E-COMMERCE CORE)**

```
┌─────────────┐                 ┌──────────────────┐
│   brands    │                 │   categories     │
├─────────────┤                 ├──────────────────┤
│ id          │                 │ id               │
│ slug        │                 │ slug             │
│ name        │                 │ name             │
│ description │                 │ parent_id        │──┐ self-ref
│ logo_url    │                 │ image_url        │  │
│ country     │                 │ is_active        │  │
│ is_active   │                 │ position         │  │
└─────────────┘                 └──────────────────┘  │
       │                                 │             │
       │ 1:N                         1:N │             │
       │                                 │             │
       ▼                                 ▼             │
┌──────────────────────────────────────────────────┐  │
│                   products                       │◄─┘
├──────────────────────────────────────────────────┤
│ id, slug, brand_id, category_id                  │
│ name, sku, price, price_note                     │
│ availability_status, availability_text           │
│ description, rating, review_count                │
│ is_active, is_featured, is_new, is_limited       │
│ meta_json, created_at, updated_at                │
└──────────────────────────────────────────────────┘
       │
       ├──────────┬──────────┬──────────┬──────────┬──────────┐
       │ 1:N      │ 1:N      │ 1:N      │ 1:N      │ 1:N      │ 1:N
       ▼          ▼          ▼          ▼          ▼          ▼
┌────────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│  product_  │ │ product_ │ │ product_ │ │ product_ │ │ product_ │ │ reviews  │
│  images    │ │ specs    │ │ options  │ │highlights│ │ tabs     │ │          │
├────────────┤ ├──────────┤ ├──────────┤ ├──────────┤ ├──────────┤ ├──────────┤
│ product_id │ │product_id│ │product_id│ │product_id│ │product_id│ │product_id│
│ url        │ │group_name│ │option_   │ │icon      │ │tab_id    │ │author_   │
│ thumbnail  │ │spec_key  │ │  type    │ │title     │ │tab_label │ │  name    │
│ is_main    │ │spec_value│ │option_   │ │description│ │content  │ │rating    │
│ position   │ │position  │ │  value   │ │position  │ │position  │ │content   │
└────────────┘ └──────────┘ │price_    │ └──────────┘ └──────────┘ │verified  │
                             │modifier  │                            └──────────┘
                             └──────────┘
```

---

### **БЛОК 3: ДИНАМИЧЕСКИЕ ФИЛЬТРЫ**

```
┌─────────────────────┐
│ filter_attributes   │ (Типы фильтров)
├─────────────────────┤
│ id                  │
│ slug                │ ('brand', 'material', 'mechanism')
│ name                │ ('Бренд', 'Материал')
│ type                │ ('checkbox', 'range', 'select')
│ is_active           │
│ position            │
└─────────────────────┘
         │ 1:N
         ▼
┌─────────────────────┐
│   filter_values     │ (Значения фильтров)
├─────────────────────┤
│ id                  │
│ attribute_id        │
│ value               │ ('rolex', 'gold-18k')
│ label               │ ('Rolex', 'Золото 18К')
│ position            │
└─────────────────────┘
         │
         │ N:M (через product_filters)
         ▼
┌─────────────────────┐       ┌──────────────┐
│  product_filters    │◄─────│   products   │
├─────────────────────┤       └──────────────┘
│ product_id          │
│ filter_value_id     │
│ PRIMARY KEY (both)  │
└─────────────────────┘
```

---

### **БЛОК 4: ЖУРНАЛ**

```
┌──────────────────────┐
│ article_categories   │
├──────────────────────┤
│ id                   │
│ slug                 │
│ name                 │
│ position             │
└──────────────────────┘
         │ 1:N
         ▼
┌──────────────────────────────────────┐
│            articles                  │
├──────────────────────────────────────┤
│ id, slug, title, subtitle, excerpt   │
│ content (HTML), image_url            │
│ category_id, author_*, read_time     │
│ is_published, is_featured            │
│ published_at, views_count            │
└──────────────────────────────────────┘
         │
         │ N:M (связь с товарами)
         ▼
┌────────────────────────┐       ┌──────────┐
│ article_related_       │◄─────│ products │
│   products             │       └──────────┘
├────────────────────────┤
│ article_id             │
│ product_id             │
│ position               │
└────────────────────────┘
```

---

### **БЛОК 5: PROGRAMMATIC SEO (ГОРОДА)** 🔥

```
┌─────────────────────────────────┐
│           cities                │ (250 городов)
├─────────────────────────────────┤
│ id, slug                        │
│ name, name_genitive,            │
│   name_prepositional, etc.      │
│ region, population, timezone    │
│ delivery_days, delivery_price   │
│ hero_image_url, hero_title      │
│ meta_description, is_active     │
└─────────────────────────────────┘
         │ 1:N
         │ (125,000 - 250,000 статей!)
         ▼
┌─────────────────────────────────┐
│        city_articles            │ ⚡ ОГРОМНАЯ!
├─────────────────────────────────┤
│ id, city_id, slug               │
│ title, excerpt, content (HTML)  │
│ image_url, template_type        │
│ is_published, published_at      │
│ source_file, imported_at        │
│ views_count                     │
│ UNIQUE(city_id, slug)           │
└─────────────────────────────────┘
         │
         │ N:M (перелинковка с товарами)
         ▼
┌────────────────────────┐       ┌──────────┐
│ city_article_products  │◄─────│ products │
├────────────────────────┤       └──────────┘
│ article_id             │
│ product_id             │
│ position               │
└────────────────────────┘
```

---

### **БЛОК 6: E-COMMERCE ОПЕРАЦИИ**

```
┌─────────────────────────────────┐
│           orders                │
├─────────────────────────────────┤
│ id, order_number                │
│ customer_name, customer_phone   │
│ customer_email                  │
│ delivery_address, comment       │
│ total_amount, status            │
│ created_at, updated_at          │
└─────────────────────────────────┘
         │ 1:N
         ▼
┌─────────────────────────────────┐       ┌──────────┐
│        order_items              │◄─────│ products │
├─────────────────────────────────┤       └──────────┘
│ id, order_id, product_id        │
│ product_name (snapshot)         │
│ product_brand (snapshot)        │
│ price (snapshot), quantity      │
│ subtotal                        │
└─────────────────────────────────┘
```

---

### **БЛОК 7: SEO (УНИВЕРСАЛЬНАЯ)**

```
┌──────────────────────────────────────────┐
│              seo_meta                    │ (для ВСЕХ страниц)
├──────────────────────────────────────────┤
│ id                                       │
│ page_type, entity_id, slug               │
│ title, description, keywords             │
│ og_title, og_description, og_image       │
│ json_ld (Schema.org)                     │
│ canonical_url, noindex                   │
│ UNIQUE(page_type, entity_id)             │
└──────────────────────────────────────────┘

page_type примеры:
- 'home' (entity_id = NULL)
- 'product' (entity_id = product.id)
- 'article' (entity_id = article.id)
- 'city_page' (entity_id = city.id)
- 'city_article' (entity_id = city_article.id)
- 'static_page' (entity_id = page.id)
```

---

### **БЛОК 8: ГЛАВНАЯ СТРАНИЦА**

```
┌─────────────────┐
│   home_hero     │
├─────────────────┤
│ tagline, title  │
│ description     │
│ cta_json        │
│ stats_json      │
│ image_url       │
│ brands_json     │
└─────────────────┘

┌─────────────────┐       ┌──────────────────┐
│  collections    │──────│collection_products│──┐
├─────────────────┤       ├──────────────────┤  │
│ id, slug        │       │ collection_id    │  │
│ category, title │       │ product_id       │  │
│ description     │       │ position         │  │
│ image_url       │       └──────────────────┘  │
│ link_*          │                             │
│ position        │                             │
└─────────────────┘                             │
                                                ▼
                                         ┌──────────┐
                                         │ products │
                                         └──────────┘

┌─────────────────┐
│ home_services   │
├─────────────────┤
│ icon_svg        │
│ title           │
│ description     │
│ link_*          │
│ position        │
└─────────────────┘

┌─────────────────┐
│ testimonials    │
├─────────────────┤
│ name, position  │
│ avatar_url      │
│ text, choice    │
│ display_order   │
└─────────────────┘
```

---

### **БЛОК 9: EMAIL & СТАТИЧЕСКИЕ СТРАНИЦЫ**

```
┌──────────────────┐
│ email_templates  │
├──────────────────┤
│ template_key     │
│ subject          │
│ body_html        │
│ body_text        │
│ is_active        │
└──────────────────┘

┌─────────────┐
│    pages    │ (about, contacts, delivery и т.д.)
├─────────────┤
│ slug        │
│ title       │
│ content     │
│ template    │
│ meta_json   │
└─────────────┘
```

---

## 🔗 ОСНОВНЫЕ СВЯЗИ (RELATIONSHIPS)

### **One-to-Many (1:N)**
- `brands` → `products` (1 бренд → много товаров)
- `categories` → `products` (1 категория → много товаров)
- `products` → `product_images` (1 товар → много изображений)
- `products` → `product_specs` (1 товар → много характеристик)
- `products` → `reviews` (1 товар → много отзывов)
- `cities` → `city_articles` (1 город → 500-1000 статей) 🔥
- `orders` → `order_items` (1 заказ → много позиций)
- `filter_attributes` → `filter_values` (1 атрибут → много значений)

### **Many-to-Many (N:M)**
- `products` ↔ `filter_values` (через `product_filters`)
- `products` ↔ `collections` (через `collection_products`)
- `articles` ↔ `products` (через `article_related_products`)
- `city_articles` ↔ `products` (через `city_article_products`)

### **Self-Reference**
- `categories.parent_id` → `categories.id` (иерархия категорий)
- `navigation_items.parent_id` → `navigation_items.id` (submenu)

---

## 📈 ИНДЕКСЫ ДЛЯ ПРОИЗВОДИТЕЛЬНОСТИ

### **Критичные индексы:**

**products (9,000 записей):**
```sql
CREATE INDEX idx_products_slug ON products(slug);
CREATE INDEX idx_products_brand ON products(brand_id);
CREATE INDEX idx_products_category ON products(category_id);
CREATE INDEX idx_products_featured ON products(is_featured, is_active);
CREATE INDEX idx_products_price ON products(price);
CREATE INDEX idx_products_active ON products(is_active, position);
```

**city_articles (125k-250k записей!) 🔥:**
```sql
CREATE INDEX idx_city_articles_city ON city_articles(city_id, is_published);
CREATE INDEX idx_city_articles_slug ON city_articles(city_id, slug);
CREATE INDEX idx_city_articles_published ON city_articles(is_published, published_at);
CREATE INDEX idx_city_articles_template ON city_articles(template_type);
```

**product_filters (до 90k записей):**
```sql
CREATE INDEX idx_product_filters_product ON product_filters(product_id);
CREATE INDEX idx_product_filters_value ON product_filters(filter_value_id);
```

**seo_meta (260k+ записей):**
```sql
CREATE INDEX idx_seo_page_type ON seo_meta(page_type, entity_id);
CREATE INDEX idx_seo_slug ON seo_meta(slug);
```

---

## 🎯 ОСОБЕННОСТИ РЕАЛИЗАЦИИ

### **1. Произвольные характеристики (product_specs)**
```
Вместо фиксированных колонок:
❌ products.material, products.diameter, products.mechanism

Используем key-value:
✅ product_specs (group_name, spec_key, spec_value)

Пример:
product_id=1, group_name="Корпус", spec_key="Материал", spec_value="Розовое золото 18К"
product_id=1, group_name="Корпус", spec_key="Диаметр", spec_value="41 мм"
product_id=1, group_name="Механизм", spec_key="Калибр", spec_value="4401"
```

### **2. Динамические фильтры**
```
filter_attributes:
  id=1, slug="brand", name="Бренд", type="checkbox"
  id=2, slug="material", name="Материал", type="checkbox"
  id=3, slug="price", name="Цена", type="range"

filter_values:
  id=1, attribute_id=1, value="rolex", label="Rolex"
  id=2, attribute_id=1, value="patek", label="Patek Philippe"
  id=3, attribute_id=2, value="gold-18k", label="Золото 18К"

product_filters:
  product_id=1, filter_value_id=1 (товар 1 → Rolex)
  product_id=1, filter_value_id=3 (товар 1 → Золото 18К)
```

### **3. Programmatic SEO - импорт из MD**
```
Workflow:
1. Генерируем MD файлы (скриптом вне БД)
2. Скрипт читает MD → парсит frontmatter + content
3. Заливаем в city_articles с template_type
4. Автоматическая перелинковка через city_article_products
```

### **4. SEO для каждой страницы**
```
Примеры записей в seo_meta:

page_type='home', entity_id=NULL
  → SEO главной страницы

page_type='product', entity_id=123
  → SEO для товара с id=123

page_type='city_article', entity_id=45678
  → SEO для статьи города с id=45678
```

### **5. Коллекции (два режима)**
```
Режим 1: Статические коллекции (только в collections)
  - 6 коллекций на главной
  - Ссылки на filtered catalog

Режим 2: Товары принадлежат коллекциям
  - collection_products (many-to-many)
  - Динамическая выборка товаров по collection_id
```

---

## 📊 СТАТИСТИКА ОЖИДАЕМОГО ОБЪЕМА

| Таблица | Примерный объем | Критичность |
|---------|-----------------|-------------|
| `city_articles` | 125,000 - 250,000 | 🔥🔥🔥 |
| `seo_meta` | ~260,000 | 🔥🔥 |
| `product_filters` | ~90,000 | 🔥 |
| `products` | 1,000 - 9,000 | ⭐ |
| `product_specs` | ~50,000 | ⭐ |
| `cities` | 250 | - |
| `articles` | 100 - 500 | - |
| Остальные | < 1000 | - |

**Итого:** ~500,000 записей в БД при полном наполнении!

---

## ✅ ГОТОВО К РЕАЛИЗАЦИИ

**Следующие шаги:**
1. ✅ Анализ зафиксирован
2. ✅ ER-диаграмма создана
3. ⏭️ SQL схема
4. ⏭️ Seed данные
5. ⏭️ Аудит агентом


# CLAUDE.ASCII — Методология проектирования веб-приложений

## Что это

Это универсальная методология проектирования веб-приложений в ASCII-формате.
Сначала проектируем "на бумаге" (ASCII-схемы в .md файлах), согласовываем, потом реализуем в коде.

**Спецификация не привязана к фреймворку или стеку.** Конкретный стек указывается в `project.md`.

---

## Зачем

- Убрать "слепые зоны" при реализации — всё описано явно
- Сократить количество переделок и уточнений
- Дать разработчику (AI или человеку) полную картину до написания кода
- Переиспользовать подход в любых проектах и на любом стеке

---

## Структура директории

```
ASCII/
  CLAUDE.ASCII.md              ← этот файл (методология, одинаковый для всех проектов)
  INDEX.md                     ← содержание проекта, статусы, порядок чтения
  project.md                   ← стек, особенности, ссылки на существующий код

  layouts/                     ← каркас сайта (ПРИОРИТЕТ при проектировании)
    headers/
      {name}.md                ← вариант шапки (структура, элементы)
      {name}.admin.md          ← что редактируется в этой шапке
    footers/
      {name}.md                ← вариант подвала (структура, элементы)
      {name}.admin.md          ← что редактируется в этом подвале
    layouts.md                 ← сборки: какой header + какой footer = какой layout

  templates/                   ← шаблоны страниц (контент)
    {name}.md                  ← что видит пользователь на странице
    {name}.admin.md            ← что редактирует админ для этой страницы

  flows/                       ← бизнес-процессы (опционально, для приложений)
    {name}.md                  ← описание процесса: шаги, условия, ошибки

  admin/                       ← общие экраны админки
    page-manager.md            ← управление страницами (создание, удаление, статусы)
    menu-manager.md            ← управление меню (несколько меню: header, footer и др.)
    site-settings.md           ← глобальные настройки (логотипы, контакты, соцсети)

  data-model.md                ← рекомендуемая схема данных (не обязательная)
```

---

## Как читать эту директорию (инструкция для AI)

1. Прочитай `CLAUDE.ASCII.md` (этот файл) — пойми методологию
2. Прочитай `project.md` — пойми стек и контекст проекта
3. Прочитай `INDEX.md` — пойми что уже есть, что нужно делать
4. **Начни с `layouts/`** — это каркас, приоритет
5. Потом читай `templates/` и `admin/`
6. Если есть `flows/` — читай для понимания бизнес-логики между экранами

---

## Фазы работы

### Фаза 1 — Layout (ПРИОРИТЕТ)

Layout — это каркас сайта: шапка, подвал, обёртка. То, что пользователь видит на КАЖДОЙ странице.
**Проектируется первым**, потому что это фундамент. Если каркас кривой — весь сайт кривой.

Компоненты layout-а независимы и комбинируются:

```
Layout "default"  = Header "main"    + Footer "main"
Layout "landing"  = Header "minimal" + Footer "main"
Layout "clean"    = (без header)     + Footer "compact"
```

Правила:
- Header и Footer — это **отдельные компоненты**, не монолит
- Проект может иметь несколько вариантов header (main, minimal, и т.д.)
- Проект может иметь несколько вариантов footer (main, compact, и т.д.)
- Layout — это **сборка**: выбираем header + footer
- Каждый шаблон страницы указывает, какой layout использует

Меню:
- Меню в header и меню в footer — это **разные меню**
- Каждое управляется отдельно через menu-manager
- Header может показывать "главное меню", footer — "меню footer"

Глобальные данные (логотип, контакты, соцсети):
- Используются в разных headers/footers
- Управляются через `admin/site-settings.md` — одно место редактирования
- Headers и footers **ссылаются** на глобальные данные, не дублируют

Если проект уже существует:
- Изучить текущий header/footer
- Описать в `layouts/headers/` и `layouts/footers/` со статусом `ЕСТЬ`

### Фаза 2 — Аудит страниц (для существующих проектов)

Если проект уже существует:
- Пройти по всем страницам/роутам
- Для каждой страницы создать `templates/{name}.md` со статусом `ЕСТЬ`
- Описать текущую структуру: секции, блоки, контент (захардкожен или динамический)

### Фаза 3 — Спецификация админки

Для layout:
- `layouts/headers/{name}.admin.md` — редактирование каждого header
- `layouts/footers/{name}.admin.md` — редактирование каждого footer
- `admin/site-settings.md` — глобальные настройки сайта

Для каждого шаблона страницы:
- `templates/{name}.admin.md`
- Маппинг: элемент на сайте → поле в админке

Для общих экранов:
- `admin/page-manager.md` — создание страниц из шаблонов
- `admin/menu-manager.md` — управление навигацией (несколько меню)

### Фаза 4 — Бизнес-процессы (опционально)

> Эта фаза нужна только если в проекте есть многошаговая логика.
> Для простых сайтов (лендинг, визитка, корпоративный сайт) — пропустить.

Flows описывают **что происходит между экранами**: цепочки действий, условия, ветвления, ошибки.

Когда нужны flows:
- Оформление заказа (корзина → данные → оплата → подтверждение)
- Регистрация / авторизация (форма → валидация → подтверждение email)
- Многошаговые формы (анкета → загрузка документов → отправка)
- Любой процесс, где результат одного шага влияет на следующий

Когда flows НЕ нужны:
- Простые CRUD-операции (создать/редактировать/удалить) — они уже описаны в admin/
- Переходы по ссылкам (навигация описана в menu-manager)
- Однокнопочные действия ("нажал сохранить — сохранилось")

### Фаза 5 — Схема данных

- Создать `data-model.md` — рекомендуемая схема
- Таблицы/коллекции, поля, типы, связи
- Пометка: "рекомендуемая, адаптируется под существующую БД"

### Фаза 6 — Согласование

- Пользователь проверяет все файлы
- Даёт фидбэк, запрашивает детализацию где нужно
- AI вносит коррективы
- Повторяем до полного согласования

### Фаза 7 — Реализация

- По согласованным спецификациям, без импровизации
- Если при реализации возникает неоднозначность — сверяемся со спецификацией
- Если спецификация не покрывает кейс — обсуждаем, дополняем спецификацию, потом реализуем

---

## Статусы элементов в INDEX.md

Каждый файл и каждый элемент внутри файлов имеет статус:

| Статус | Значение | Что делать |
|--------|----------|------------|
| `ЕСТЬ` | Уже реализовано в коде | ASCII описывает текущее состояние, не трогать |
| `НОВОЕ` | Ещё не реализовано | ASCII — спецификация для реализации с нуля |
| `ИЗМЕНЕНИЕ` | Есть в коде, но нужно доработать | Сравнить код со спецификацией, реализовать разницу |

---

## Архитектура Layout

### Принцип: компоненты, не монолиты

Layout собирается из независимых компонентов:

```
┌─ Layout ──────────────────────────────────────┐
│                                                │
│  ┌─ Header (один из вариантов) ─────────────┐ │
│  │  Логотип* | Меню (header-menu)* | CTA     │ │
│  └───────────────────────────────────────────┘ │
│                                                │
│  ┌─ Контент страницы (из шаблона) ──────────┐ │
│  │  ...                                      │ │
│  └───────────────────────────────────────────┘ │
│                                                │
│  ┌─ Footer (один из вариантов) ─────────────┐ │
│  │  Логотип* | Меню (footer-menu)* | Соцсети*│ │
│  └───────────────────────────────────────────┘ │
│                                                │
└────────────────────────────────────────────────┘

* = берётся из глобальных настроек (site-settings)
```

### Как определить что куда

```
Вопрос: "Этот элемент одинаковый на ВСЕХ страницах с данным layout?"
  ДА  → это layout (headers/ или footers/)
  НЕТ → это контент страницы (templates/)

Вопрос: "Этот элемент используется в НЕСКОЛЬКИХ headers или footers?"
  ДА  → это глобальная настройка (site-settings)
  НЕТ → это элемент конкретного header/footer
```

---

## Формат описания header/footer (layouts/headers/{name}.md)

```markdown
# Header: {Название}

Используется в layouts: {список layout-ов}

## Структура

┌─────────────────────────────────────────────┐
│  ASCII-схема header                         │
└─────────────────────────────────────────────┘

Элементы:
- {название}: {источник — глобальный / собственный}, {тип}, {ограничения}

## Поведение
- Мобильная версия: ...
- Прокрутка (sticky/fixed/static): ...
```

---

## Формат файла layouts.md

```markdown
# Сборки Layout

| Название  | Header   | Footer   | Используется в шаблонах    |
|-----------|----------|----------|---------------------------|
| default   | main     | main     | homepage, about, contacts |
| landing   | minimal  | main     | landing                   |
| clean     | (нет)    | compact  | special                   |
```

---

## Формат описания страницы (templates/{name}.md)

```markdown
# Шаблон: {Название}

Slug: /{pattern}
Layout: {название layout-а}
Масштабируемый: да / нет

## Секция: {Название секции}
[СТАТУС]

┌─────────────────────────────────────┐
│  Описание структуры секции          │
│  в ASCII-формате                    │
│  с указанием ключевых элементов     │
└─────────────────────────────────────┘

Элементы:
- {название}: {тип}, {ограничения}

Состояния:
- Загрузка: ...
- Пусто: ...
- Ошибка: ...
```

---

## Формат описания админки (templates/{name}.admin.md)

```markdown
# Админка: {Название шаблона}

## Список экземпляров (если масштабируемый)

┌──────────────────────────────────────────────┐
│ Название        | Slug       | Статус | Действия │
│ ...             | ...        | ...    | [✏️][🗑️] │
│                                              │
│ [+ Создать страницу]                         │
└──────────────────────────────────────────────┘

## Редактирование

### Секция: {Название}

┌─────────────────────────────────────────┐
│ Поле:       [тип поля]  ограничения     │
└─────────────────────────────────────────┘

### Действия

[Сохранить] [Предпросмотр] [Отменить]

## Сценарии использования

СЦЕНАРИЙ: {название}
  1. Админ ...
  2. Видит ...
  3. Делает ...
  4. Результат: ...

ОШИБКИ:
  - {условие} → {сообщение / поведение}
```

---

## Формат схемы данных (data-model.md)

```markdown
# Схема данных (рекомендуемая)

> Адаптируется под существующую БД проекта.
> Если БД уже есть — сверить и дополнить, а не заменять.
```

---

## Концепция шаблонов страниц

Каждая страница сайта создаётся из шаблона. Шаблон определяет:
- какие секции есть на странице
- какие поля в каждой секции
- какие типы данных и ограничения
- **какой layout используется**

Шаблон бывает:
- **Единичный** (масштабируемый: нет) — только один экземпляр (например, главная)
- **Масштабируемый** (масштабируемый: да) — можно создать N экземпляров (например, услуги)

Новые шаблоны создаёт разработчик. Админ создаёт экземпляры страниц из существующих шаблонов.

---

## Бизнес-процессы (flows/)

> Опциональный раздел. Создаётся только при наличии многошаговой логики.

### Что описывают flows

Flows закрывают пробел между экранами. Templates описывают **что на экране**. Flows описывают **что происходит между экранами и внутри процессов**.

```
Без flows:
  Экран "Корзина" → ??? → Экран "Заказ оформлен"

С flows:
  Экран "Корзина" → проверка наличия → ввод данных →
  → выбор доставки → оплата → подтверждение →
  → Экран "Заказ оформлен"
```

### Формат описания (flows/{name}.md)

```markdown
# Flow: {Название процесса}

Триггер: {что запускает процесс}
Результат: {что получается в конце}
Участники: {кто участвует — пользователь, админ, система}

## Шаги

### Шаг 1: {Название}
Экран: {ссылка на template, если есть}
Действие: {что делает участник}
Данные: {что вводится / отображается}
→ Успех: перейти к Шагу 2
→ Ошибка: {что происходит}

### Шаг 2: {Название}
...

## Схема процесса

{Начало}
    ↓
[Шаг 1: действие]
    ↓
<Условие?>──НЕТ──→ [Ошибка / альтернатива]
    ↓ ДА
[Шаг 2: действие]
    ↓
{Результат}

## Ошибки и граничные случаи

| Ситуация | Что происходит |
|----------|---------------|
| {описание} | {реакция системы} |
```

### Как определить нужен ли flow

```
Вопрос: "Между действием пользователя и результатом есть промежуточные шаги?"
  ДА  → нужен flow
  НЕТ → достаточно сценария в .admin.md

Вопрос: "Результат зависит от условий (оплата прошла / не прошла)?"
  ДА  → нужен flow
  НЕТ → достаточно сценария в .admin.md
```

---

## Уровень детализации

- По умолчанию: средний уровень — секции + ключевые элементы + поведение
- Без описания каждого пикселя и отступа
- Достаточно для реализации без угадывания
- Если пользователь запрашивает детализацию конкретного элемента — дополняем

---

## Работа с существующим проектом

Если проект уже существует и нужно добавить/расширить функционал:

1. Изучить существующий код (роуты, компоненты, API, БД)
2. **Начать с layout** — описать текущие header/footer со статусом `ЕСТЬ`
3. В ASCII-файлах пометить существующие элементы статусом `ЕСТЬ`
4. Пометить что добавляется (`НОВОЕ`) и что меняется (`ИЗМЕНЕНИЕ`)
5. Если в проекте уже есть админка — изучить её подход и следовать ему
6. В `project.md` указать ссылки на ключевые файлы/директории проекта

---

## Важно

- Эта методология — инструмент проектирования, не догма
- Если какой-то файл/раздел не нужен для конкретного проекта — пропустить
- Если нужно что-то дополнительное — добавить
- Цель: ясность и предсказуемость при реализации
